{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Inicio{% endblock %}

{% block body %}
    {# {{ dump(post) }} #}
    <div class="container">

        <div class="row g-3 mt-5">
            <div class="col-md-5 col-lg-4 order-md-last d-none d-md-block">
                <h4 class="d-flex justify-content-between align-items-center mb-3">
                    <span class="text-muted">Ultimos post</span>
                </h4>
                {#TODO: Agregar los ultimos 5 posts                #}
                <ul class="list-group mb-3">
                        <h1>hola</h1>
                </ul>
            </div>
            <div class="col-md-7 col-lg-8">
                <h4 class="mb-3">POST DE INTERES</h4>
                {% for item in post  %}
                    <div class="card mb-3" style="width:30rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ item.title }}</h5>
                            <p class="card-text">{{ item.detail|slice(0,400)}}... </p>
                            <a href="{{ path('detail_post') }}/{{ item.id }}" class="btn btn-primary">Card link</a>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>

    </div>
    {# <div id="post"> #}

    {# <div class="container-fluid"> #}
    {# {% for item in post %} #}
    {# <div class="card mb-2" style="width: 25rem;"> #}
    {# <div class="card-body"> #}
    {# <h5 class="card-title">Card title</h5> #}
    {# <h6 class="card-subtitle mb-2 text-muted">Card subtitle</h6> #}
    {# <p class="card-text">Some quick example text to build on the card title and make up the bulk of #}
    {# the card's content.</p> #}
    {# <a href="#" class="card-link">Card link</a> #}
    {# <a href="#" class="card-link">Another link</a> #}
    {# </div> #}
    {# </div> #}
    {# {% endfor %} #}

    {# </div> #}
    {# </div> #}

{% endblock %}
